# Python_RINTD

Minteck wants us to do spectral unmixing and achieve identification of three mineral phases. We currently have two problems we assumed we could move from pure spectral 
elements from the paper https://zenodo.org/records/1476495 but truth is we just cant get results that way. In reality we need to change datasets HIDSAG https://www.nature.com/articles/s41597-023-02061-x and https://github.com/EleftheriaTtl/minerals-in-the-wild Minerals in the wild are our best bets.

But now so we know that Tucker decomposition cannot effectively un mix these spectral data cubes to get end members for spectral un mixing. We want to achieve real time spectral un mixing. Meaning we need an input method that allows us to process HSI data incrementally without bottle necking so full tucker compression off the table if we do implement Tucker representation the only way forward is maybe partial tucker decomposition OR Online NMF or Incremental PCA ( Maybe Andy had a point ). Basically we cant process HSI cubes from a file, it just wont work so partial Tucker or those Incremental algorithms are the choices, but do not assume coherence between data cubes assume the data cubes are all non coherent they dont share overlap so dimensional reduction is per data cube. Once you have a low dimensional representation you spectrally un mix and you tell us how much of what is in a datacube. 

Our current implementation had tried to extract the individual mineral spectra using Tucker decomposition but we can extract say 1 which is what the classify.py code tries to dousing the sam_library.npz library but again it was just a way to do it simpler then complicate things. Problems that arose there even for pure mineral spectra it achieved pure classification even with minerals that we knew are supposed to show up in a region (SWIR) so this already shows us we will struggle even if we increase the rank. Higher rank would mean we can extract more respective minerals so tricky there. We were pushing for novelty so that i guess was the downfall. So that is the limit of current implementation of full Tucker decomposition. So we need a incremental dimension reduction algorithm of spectral data that allows real time spectral un mixing is ideally the correct thing.

**GODSPEED**
